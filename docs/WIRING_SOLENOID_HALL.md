# Подключение бистабильного соленоида и датчиков Холла

## 🔌 Подключение L298N (модуль zx-040) для бистабильного соленоида RSF22/08-O035

### Схема подключения L298N к ESP32

```
ESP32        L298N (zx-040)
-----        ---------------
GPIO25   ->  IN1 (управление направлением)
GPIO26   ->  IN2 (управление направлением)
GPIO15   ->  ENA (включение, можно использовать PWM)
GND      ->  GND

L298N OUT1  ->  Клемма 1 бистабильного соленоида RSF22/08-O035
L298N OUT2  ->  Клемма 2 бистабильного соленоида RSF22/08-O035

L298N GND   ->  GND (общий с ESP32)
L298N VSS   ->  24V (питание соленоида) - НЕ ПОДКЛЮЧАТЬ К ESP32!
```

### ⚠️ ВАЖНО: Питание L298N для 24V системы:

#### Для 24V (соленоид 24V) - ОБЯЗАТЕЛЬНО:
```
1. СНЯТЬ перемычку 5V_EN на L298N (отключить встроенный стабилизатор 5V)
   ⚠️ Без этого стабилизатор перегреется и сгорит!

2. L298N VCC -> 5V от внешнего источника (НЕ от ESP32!)
   - ESP32 работает на 3.3V, не подходит для L298N VCC
   - Используйте отдельный источник 5V (например, USB-адаптер или стабилизатор 5V)
   - Или используйте стабилизатор 5V от общего 24V источника (LM7805 + радиатор)
```

**L298N поддерживает VSS до 35V**, но при напряжении выше 12V встроенный стабилизатор перегревается, поэтому его нужно отключить!

### ⚠️ ВАЖНО для бистабильного соленоида:

1. **Короткий импульс**: Бистабильный соленоид требует короткий импульс (50-200ms) для переключения
2. **Направление**: 
   - IN1=HIGH, IN2=LOW → переключение в состояние A
   - IN1=LOW, IN2=HIGH → переключение в состояние B
3. **После импульса**: Соленоид остается в переключенном состоянии БЕЗ питания
4. **Питание L298N**: VSS должен быть подключен к источнику питания 24V (НЕ к ESP32!)

### Параметры соленоида RSF22/08-O035:
- Напряжение: 24V (для вашей системы)
- Ток: ~200-300mA (во время импульса)
- Тип: Бистабильный (остается в состоянии после импульса)

---

## 🧲 Подключение датчиков Холла AH3134

### Схема подключения датчиков Холла к ESP32

```
ESP32        Датчик Холла AH3134 (Датчик 1)
-----        ------------------------------
GPIO32   ->  OUT (выходной сигнал)
5V       ->  VCC (питание +5V)
GND      ->  GND (общий провод)

ESP32        Датчик Холла AH3134 (Датчик 2)
-----        ------------------------------
GPIO33   ->  OUT (выходной сигнал)
5V       ->  VCC (питание +5V)
GND      ->  GND (общий провод)
```

### ⚠️ ВАЖНО для датчиков Холла:

1. **Активный LOW**: Датчики работают по принципу "активный низкий"
   - При поднесении магнита → OUT = LOW (0V)
   - При отсутствии магнита → OUT = HIGH (3.3V/5V)

2. **Подтягивающий резистор**: Используется встроенный INPUT_PULLUP ESP32

3. **Питание**: Датчики AH3134 работают от 3.3V-5V (используем 5V от ESP32)

4. **Чувствительность**: Датчики реагируют на магнитное поле (северный или южный полюс)

### Распиновка датчика AH3134 (TO-92UA):
```
Вывод 1 (VCC)  ->  +5V
Вывод 2 (GND)  ->  GND
Вывод 3 (OUT)  ->  GPIO32 (датчик 1) или GPIO33 (датчик 2)
```

---

## 📋 Полная таблица подключений

| Компонент | ESP32 Пин | Назначение | Примечание |
|-----------|-----------|------------|------------|
| **L298N IN1** | GPIO25 | Управление направлением | Для состояния A |
| **L298N IN2** | GPIO26 | Управление направлением | Для состояния B |
| **L298N ENA** | GPIO15 | Включение | PWM опционально |
| **L298N GND** | GND | Общий провод | Обязательно! |
| **Датчик Холла 1 OUT** | GPIO32 | Выходной сигнал | INPUT_PULLUP |
| **Датчик Холла 1 VCC** | 5V | Питание | +5V |
| **Датчик Холла 1 GND** | GND | Общий провод | Общий GND |
| **Датчик Холла 2 OUT** | GPIO33 | Выходной сигнал | INPUT_PULLUP |
| **Датчик Холла 2 VCC** | 5V | Питание | +5V |
| **Датчик Холла 2 GND** | GND | Общий провод | Общий GND |

---

## 🔧 Настройка в коде

Все пины уже настроены в `src/pins.h`:

```cpp
// L298N пины
#define SOLENOID_IN1_PIN  25   // GPIO25
#define SOLENOID_IN2_PIN  26   // GPIO26
#define SOLENOID_ENA_PIN  15   // GPIO15

// Датчики Холла
#define HALL_SENSOR_1_PIN 32   // GPIO32
#define HALL_SENSOR_2_PIN 33   // GPIO33
```

---

## 🧪 Тестирование

### Тест соленоида:
1. Откройте веб-интерфейс (192.168.4.1)
2. Найдите секцию "🔌 Управление бистабильным соленоидом"
3. Нажмите "🔌 Переключить в состояние A" или "🔌 Переключить в состояние B"
4. Соленоид должен переключиться (короткий щелчок)

### Тест датчиков Холла:
1. Откройте веб-интерфейс (192.168.4.1)
2. Найдите секцию "🧲 Датчики Холла"
3. Поднесите магнит к датчику 1 → статус должен измениться на "✅ МАГНИТ ОБНАРУЖЕН"
4. Поднесите магнит к датчику 2 → статус должен измениться на "✅ МАГНИТ ОБНАРУЖЕН"
5. Уберите магнит → статус вернется к "❌ НЕТ МАГНИТА"

---

## ⚡ Питание

### L298N для 24V системы:

- **VCC (логика)**: 5V от внешнего источника (НЕ от ESP32!)
  - ESP32 работает на 3.3V, не подходит для L298N
  - Используйте отдельный источник 5V или стабилизатор от 24V (LM7805)
- **VSS (мотор)**: 24V внешний источник (НЕ подключать к ESP32!)
- **GND**: Общий с ESP32
- **⚠️ ОБЯЗАТЕЛЬНО СНЯТЬ перемычку 5V_EN** (отключить встроенный стабилизатор!)
  - Без этого стабилизатор перегреется при 24V и сгорит!

### Датчики Холла:
- **VCC**: 5V от ESP32 (достаточно для 2 датчиков)
- **GND**: Общий с ESP32

---

## 🛠️ Устранение неполадок

### Соленоид не переключается:
- Проверьте подключение L298N к ESP32 (IN1, IN2, ENA, GND)
- Проверьте питание L298N VSS (24V)
- Проверьте питание L298N VCC (5V логика от внешнего источника)
- **ОБЯЗАТЕЛЬНО**: Убедитесь, что перемычка 5V_EN снята (иначе стабилизатор сгорит!)
- Проверьте подключение соленоида к OUT1 и OUT2
- Увеличьте длительность импульса в веб-интерфейсе (50-500ms)

### Датчики Холла не реагируют:
- Проверьте подключение (OUT, VCC, GND)
- Проверьте, что используется правильный полюс магнита
- Проверьте питание 5V
- Убедитесь, что магнит достаточно близко к датчику

---

## 📝 API Endpoints

### Управление соленоидом:
- `POST /api/solenoid/switch_a?duration=100` - Переключить в состояние A
- `POST /api/solenoid/switch_b?duration=100` - Переключить в состояние B
- `GET /api/solenoid/status` - Получить состояние соленоида

### Датчики Холла:
- `GET /api/hall_sensors` - Получить состояние обоих датчиков

Все данные также включены в основной статус: `GET /api/status`

